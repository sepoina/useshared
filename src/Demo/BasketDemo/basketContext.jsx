/* eslint-disable react-refresh/only-export-components */
/* eslint-disable react/prop-types */
import { sharedCreateContext } from '@sepoina/useshared';

const basketContext = sharedCreateContext(); // create new store for basket ball

export default basketContext;
